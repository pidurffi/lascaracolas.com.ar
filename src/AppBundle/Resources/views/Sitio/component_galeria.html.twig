<div class="col-lg-6">
    <div class="ficha-carousel owl-carousel owl-theme">
    	{% for gal in galeria.galeria %}
        <div class="item"><img src="{{ asset('/uploads/galerias/gallery/' ~ gal) }}"></div>
        {% endfor %}
    </div>
</div>
